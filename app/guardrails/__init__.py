"""analysis_guard.py (Phase 13): the confidence gate — citation integrity,
numeric grounding, and the reliability-tier rule (ARCHITECTURE.md §1) — a
BULLISH/BEARISH stance needs >=1 citation with reliability_tier>=3 —
combined into confidence + quality_status. Also check_input_relevance,
run before retrieval/generation start, for execution-shaped requests."""
