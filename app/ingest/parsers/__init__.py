"""Phase 3-4: one parser per source format — quotes_parser.py,
edgar_parser.py, news_parser.py, reddit_parser.py. Each produces an
intermediate, parser-specific representation; normalizers/ converts that
to the canonical Document."""
