"""ONLINE pipeline (Phase 10): sql_retriever.py (Axis 2, typed reads) and
vector_retriever.py (Axis 3, top-k + threshold + soft-fail). Reads what
ingest/ already wrote; never triggers a fetch itself."""
