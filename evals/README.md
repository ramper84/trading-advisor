# Evals

`golden_queries.json` lands in Phase 2, seeded from `README.md` §2a —
including the out-of-scope execution case, an abstention case, and at least
one case that specifically requires the vector retriever (Axis 3) to
distinguish it from a pure Axis-2 regression. Not written yet; this
directory exists so Phase 1's tree matches `CLAUDE.md` §3.
